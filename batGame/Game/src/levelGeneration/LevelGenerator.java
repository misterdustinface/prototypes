package levelGeneration;

public class LevelGenerator {

	// make static bounds (BoundGenerator)
	// contains the "cylinder" - boundsbuffers
	// takes portions of the cylinder and manipulates the data by converting into "spikes" (SpikeGenerator)
	
	// etc.
	
}
